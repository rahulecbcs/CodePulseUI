//ye wali model help karege edit category form pe data collect krne me, issi structure me data jayega api k pass, save k click pe

export interface UpdateCategoryRequest {
     name: string;
     urlHandle : string;
}